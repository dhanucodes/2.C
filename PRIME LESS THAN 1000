#include<stdio.h>
void main()
{
    for(int i=2;i<1000;i++)
    {
        count=0;
        for(int a=2;a<i;a++)
        {
            if(a%i==0)
                count=count+1;
        }
        if(count==0)
            printf("%d",a);
    }
}
